export default function Loading() {
  return (
    <main className="flex min-h-screen items-center justify-center p-6">
      <div className="panel-surface rounded-[30px] px-8 py-10 text-center">
        <p className="text-xs uppercase tracking-[0.35em] text-ember-300/80">Cinematic Chess</p>
        <h1 className="mt-4 font-[var(--font-display)] text-4xl font-semibold">Preparing the battlefield.</h1>
      </div>
    </main>
  );
}
